//! Built-in [`Constraint`](koakuma_core::traits::Constraint) implementations.
//!
//! M1.1 wires these to the Registry so `ConstraintExpr::evaluate` can dispatch.
//! M2.2 adds full DSL expression support via `koakuma-script` (Rhai).
